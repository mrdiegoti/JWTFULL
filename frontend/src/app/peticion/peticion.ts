export interface Peticion {
    id?: number;
    titulo: string;
    descripcion: string;
    destinatario: string;
    categoria_id: number;
    // Agrega otros campos que tengas en la BD, como fecha, estado, firmantes, etc.
  }
  